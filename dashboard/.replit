run = "npm start"
entrypoint = "app/page.tsx"

[nix]
channel = "stable-24_05"

[env]
PORT = "3000"

[deployment]
visibility = "private"
run = ["sh", "-c", "npm start"]
build = ["sh", "-c", "npm install && npm run build"]

[[ports]]
localPort = 3000
externalPort = 80
